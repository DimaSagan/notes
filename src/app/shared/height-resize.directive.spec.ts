import { HeightResizeDirective } from './height-resize.directive';

describe('HeightResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightResizeDirective();
    expect(directive).toBeTruthy();
  });
});
