import { RedaktorKeysDirective } from './redaktor-keys.directive';

describe('RedaktorKeysDirective', () => {
  it('should create an instance', () => {
    const directive = new RedaktorKeysDirective();
    expect(directive).toBeTruthy();
  });
});
